// 根包：字幕库本体。所有源文件都在这个目录下，共用这一个 moon.pkg。
//
// quickcheck 只在属性测试里用，用 `for "wbtest"` 限定范围，不让它跟着库被下游引入。
//
// 目标名写 "wbtest" 而不是 "test"：属性测试放在 `*_wbtest.mbt`（白盒测试）里，
// 因为要构造 `Cue` 这种包内类型。写成 "test" 的话编译器会报"quickcheck 未引入"，
// 同时又报这个声明没用 —— 两个方向都提示目标名不对。

import {
  "moonbitlang/core/quickcheck",
} for "wbtest"
