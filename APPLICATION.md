# 项目申报书

**项目名称**：moonbit-subtitle（SRT / WebVTT 字幕解析与时间轴处理库）

**GitHub 仓库**：https://github.com/AYHEY001/moonbit-subtitle

---

## 一、项目简介

moonbit-subtitle 用来读写 SRT 与 WebVTT 字幕文件：把文件解析成带起止时间的数据结构，对时间轴做平移、缩放、合并重叠等变换，再按原格式渲染回去。另附一个命令行工具，处理平移与格式转换。

实际拿到的字幕文件大多不干净：从字幕组、剪辑软件、在线平台导出的 SRT 里，常见的有开头带 BOM、换行是 CRLF、序号跳号或重复、相邻两条时间区间重叠几十毫秒、时间行两侧留着空格。现成工具碰到这些通常直接报错，或者把解析不了的条目丢掉且不做提示，使用者往往到成片导出之后才发现少了几行字幕。本项目把这些杂质逐项识别并修复，修不掉的地方按行号明确指出。

## 二、项目方向与通用性

项目属于开发者工具中的文本格式处理方向。格式规范本身不含语言或平台的限定，这套解析逻辑与具体的剪辑软件、操作系统无关。

库不引入第三方依赖，只用 MoonBit 核心库的 `String`、`Array`、`argparse`、`test`、`quickcheck`，引入方不必担心依赖树变大。时间轴变换这一层处理的是带起止时间的有序文本段，除了字幕，音频章节标记和剪辑点清单这类数据也能直接套用。

## 三、预期使用场景

**场景一：视频产线的字幕自动对齐**

我目前在维护一条自动生成新闻短视频的流水线。配音由合成音频产出，时长和写稿时按字数估算的往往对不上，字幕需要照音频的实际长度重算：整体平移若干毫秒，再裁掉超出片尾的部分。这一步现在靠手工在剪辑软件里拖动，每天要花十几分钟。接入这个库之后可以写成脚本，按每条音频的实际时长自动算平移量。

**场景二：多语言字幕的版本对齐**

一条片子做完中文字幕后再出英文字幕，翻译通常对着中文稿的顺序进行，时间戳直接沿用。中文字幕改动之后，两份时间轴就错开了。用这个库分别解析两份文件，按文本内容匹配到对应条目，把中文那边的时间轴映射到英文版上，不必在编辑器里逐条调整。

**场景三：跨平台字幕格式转换**

同一份字幕交给不同平台时格式要求不同：B 站和 YouTube 用 SRT，网页播放器多要 WebVTT，部分剪辑软件只认特定的序号写法。在线转换站点需要上传文件，客户素材或未公开内容不便外传。命令行工具在本地完成转换，顺带把序号规范化、按开始时间重排。

## 四、拟实现的核心功能

- 时间戳的解析与格式化，内部统一用毫秒整数表示，避免浮点秒数在连续变换中累积误差
- SRT 解析，容忍 BOM、CRLF、行尾空格、跳号与重复序号
- WebVTT 解析，支持 `WEBVTT` 头、注释与样式块、cue 标识
- 渲染回 SRT 与 WebVTT，输出符合各自规范的文件
- 时间轴变换：整体平移、按倍速缩放、相邻重叠合并、按开始时间重排、序号规范化
- 命令行工具：时间轴平移、格式转换

## 五、项目性质

本项目为原创项目，代码全部自行编写，未移植或改编任何既有项目。

SRT 没有官方标准，按业内通行的写法实现；WebVTT 依据 W3C 的 WebVTT 规范。这两份都是格式规范，不涉及代码许可。

第七条「移植项目说明」因此不适用。
